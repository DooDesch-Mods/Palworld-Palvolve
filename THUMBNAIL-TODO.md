# thumbnail.png fehlt absichtlich

Der Workshop verlangt ein `thumbnail.png` (< 1 MB). Es wird NICHT generisch erzeugt:
echtes Artwork beim User anfordern (256x256 oder größer, quadratisch), als
`thumbnail.png` neben die Info.json legen und diese Datei löschen.
